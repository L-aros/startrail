name: application
publish_to: none
description: 拾星迹应用用例与状态编排层。
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  domain:
    path: ../domain
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.32.0
