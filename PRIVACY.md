# 隐私说明（实现约束，非法律意见）

默认模式只在设备本地处理数据。启用 R2 同步后，客户端向用户选择的端点上传加密后的 Vault 对象；服务端仍可观察请求元数据（IP、对象大小、频率），所以客户端会填充对象并避免语义化对象名。项目不运营账户服务。

诊断默认关闭；启用后仅发送 `docs/operations/TELEMETRY.md` 定义的匿名聚合事件，绝不发送内容、标题、标签、文件名、对象 ID、密钥、密码或 R2 地址。用户可随时关闭并清除本地诊断队列。应用不内置广告、追踪 SDK 或第三方分析 SDK。
