# 日志

日志默认本地环形缓冲，开发版可导出；发行版默认不持久化 debug 日志。字段只允许：时间、级别、组件、错误码、操作类型、耗时桶、结果、匿名本次会话 ID。禁止内容、标题、标签、文件名、路径、URL、IP、凭据、密码、密钥、对象/manifest ID、SQL、原始异常。

日志生命周期：内存会话结束清除；用户主动导出的诊断包须预览/脱敏并由用户选择保存位置。测试用规则扫描禁止字段与 secret pattern。
