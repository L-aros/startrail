# UI 导航

底部/侧栏主入口：**时间线**、**记录**、**回忆**、**设置**。时间线可进入条目详情与编辑；记录是新建入口；回忆展示本地筛选结果；设置包含 Vault、同步、隐私、更新、关于。

关键状态：锁定时只显示解锁/创建/恢复；同步冲突在时间线和设置均有非阻塞徽标；危险操作（删除 Vault、忘记本机同步凭据、恢复覆盖）必须二次确认并说明后果。Windows 支持快捷键：Ctrl+N 新条目、Ctrl+S 保存、Ctrl+F 本地搜索、Esc 关闭表单。
