# AGENTS.md — StarTrail 开发契约

## 优先级

1. 用户数据安全、可恢复性与隐私；2. 已发布格式/协议兼容性；3. 离线可用；4. 功能与体验；5. 代码简洁。发生冲突时按此顺序取舍并记录 ADR。

## 必须遵守

- UI 显示“拾星迹”；仓库、包、协议命名可用 `startrail`。不得将 `StarTrail` 作为正式英文品牌承诺。
- Vault 内容、索引、缩略图、对象名、同步清单均视为敏感；不写入日志、崩溃报告或遥测。
- 密钥只存在内存与平台安全存储的包装材料中；永不提交、永不打印、永不上传。密码不可恢复。
- 所有持久化写入使用写临时文件 → fsync → 原子替换；变更前保留可验证回滚路径。
- 所有网络请求默认关闭；同步需要用户显式启用且可取消。网络层不得绕过加密层。
- 只把密文对象上传到 R2。服务器不可获知条目、路径、时间线标题或内容。
- 已发布的 `vault_format_version`、对象编码、加密 AD、manifest 语义只能追加兼容；破坏性变更须新版本、迁移和 ADR。
- 依赖新增须记录许可证、用途、平台支持与安全影响到 `THIRD_PARTY.md`。

## 实现边界

- Flutter/Dart 是唯一共享产品层；Android 与 Windows 平台 API 必须封装在 adapter 接口后。
- 领域层不依赖 Flutter Widget、HTTP、文件系统或插件；UI 不直接访问数据库/R2。
- 不把数据库文件、明文导出、测试 Vault、`.env`、R2 token、签名密钥提交到 Git。
- 先写或更新单元/集成测试；任何 bug 修复必须有可复现测试。

## 提交前门禁

`dart format`、静态分析、相关单元测试、迁移/同步回归测试通过；检查日志与 fixture 无真实私密内容；更新 CHANGELOG 与文档。涉及协议或安全边界时更新 ADR、威胁模型、测试向量和版本号。
